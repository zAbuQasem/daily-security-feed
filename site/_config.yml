title: Security Feed Monitor
description: AI-curated security research feed — auto-updated daily

url: "https://zabuqasem.github.io"
baseurl: "/security-feed-monitor"

github:
  owner_name: zAbuQasem
  repository_name: security-feed-monitor

permalink: /posts/:title/

defaults:
  - scope:
      path: ""
      type: posts
    values:
      layout: post

exclude:
  - Gemfile
  - Gemfile.lock
  - "*.gem"
