title: Security Feed Monitor
description: AI-curated security research feed — auto-updated daily
remote_theme: pages-themes/hacker@v0.2.0

permalink: /:year/:month/:day/:title/

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"

exclude:
  - Gemfile
  - Gemfile.lock
